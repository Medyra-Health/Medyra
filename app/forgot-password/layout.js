export const metadata = {
  title: 'Reset Password',
  description: 'Reset the password for your Medyra account.',
  robots: { index: false, follow: false },
}

export default function ForgotPasswordLayout({ children }) {
  return children
}
